# Diabetes Prediction Webapp
A machine learning web application that predicts whether a person is diabetic based on diagnostic health measurements. Built using a Support Vector Machine (SVM) classifier trained on the PIMA Indian Diabetes Dataset.

